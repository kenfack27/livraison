export interface StoreUpdateRequest {
    name: string;
    phoneNumber: string;
    email: string;
    address: string;
    id: number;
    approveOrder: boolean;
     // latitude: number;
    // longitude: number;
}
